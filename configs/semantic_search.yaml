semantic_search:
  enabled: true
  embedding_model: "jinaai/jina-code-embeddings-0.5b"
  reranker_model: null
  device: "cuda"
  max_indices: 50
  
  # Prompt configuration
  prompts:
    with_semantic_search:
      system_prompt: "src/prompts/system_prompt.py"
    without_semantic_search:
      system_prompt: "src/prompts/system_prompt.py"
